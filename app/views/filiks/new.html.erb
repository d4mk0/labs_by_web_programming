<h1>New filik</h1>

<%= form_for :filik, url: filiks_path, multipart: true do |f| %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.file_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', filiks_path %>
